# Data pipeline (phase 1) — shared by both tracks
nflreadpy>=0.1.5
pandas>=2.2
numpy>=2.0
pyarrow>=15.0
scikit-learn>=1.5

# MLP track (phases 2-4): Keras -> QKeras -> hls4ml
tensorflow>=2.21
keras>=3.14
qkeras==0.9.0
hls4ml==1.3.0

# GBDT track (phases 2, 4): XGBoost -> conifer
xgboost>=3.0
conifer==1.9

# Verification (phase 6) and deployment (phase 7)
cocotb>=2.0
pyserial>=3.5

# Tests and plots
pytest>=8.0
matplotlib>=3.9
seaborn>=0.13

# Toolchain versions are pinned where the integration is fragile:
#   qkeras 0.9.0   - needs tf.config.run_functions_eagerly(True) under Keras 3
#   hls4ml 1.3.0   - the QKeras workaround in mlp/phase4_hls/convert.py targets this
#   conifer 1.9    - ships broken templates that gbdt/phase4_hls/convert_hls.py patches
# conifer also needs nlohmann/json and Xilinx ap_types dropped into
# site-packages/conifer/external/ before compile() works. See docs/BUILD.md.
